using UnityEngine;

public class AdurasMonobehavius : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    public void Printf(string message)
    {
        Debug.Log(message);
    }
}
